# las tuplas no pueden cambiarse
# no son objetos

numbers = (1,2,3,4,5,24)


print(numbers.count(4)) #devuelve las veces que aparece el valor dentro de la tupla

print(numbers.index(24)) #devuelve la posición de la primera ocurrencia
